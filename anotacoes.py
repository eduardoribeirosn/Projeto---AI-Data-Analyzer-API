# Importações das bibliotecas a serem utilizadas (flask, pandas, langgraph)

# Criação da Class (class -> TypedDict)

# Ligação do Flask (app = Flask(__name__)

# Funções/nodes

# função buildar graph (instânciar, nodes, edges e compilar)

# variável global chamando o buildar

# Rota do flask (post, invoke graph, return)

# if __name__ == "__main__": app.run(debug=True)